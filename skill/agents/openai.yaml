interface:
  display_name: "GA4 Web Analyst"
  short_description: "Create evidence-led, source-grounded GA4 tracking plans"
  default_prompt: "Use $ga4-tracking-plan as a web analyst to build a quality-first GA4 specification: decide language, inspect real journeys and finite values, resolve each selected event's current official semantics and parameter table, prefer applicable official parameters when evidence and use justify them, add custom fields only for a documented official gap, preserve client templates strictly, and deliver a readable tracking plan."
