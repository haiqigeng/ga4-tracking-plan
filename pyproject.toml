[project]
name = "ga4-tracking-plan-skill"
version = "2.8.0"
description = "Utility-first GA4 web analyst skill for human-readable, implementation-ready tracking plans"
requires-python = ">=3.11"
dependencies = [
  "jsonschema>=4.18,<5",
  "openpyxl>=3.1,<4",
  "pillow>=10,<13",
  "playwright>=1.45,<2",
]

[project.optional-dependencies]
dev = [
  "coverage>=7.9,<8",
  "pip-audit>=2.9,<3",
  "radon>=6,<7",
  "ruff>=0.12,<1",
  "vulture>=2.14,<3",
]

[tool.ruff]
target-version = "py311"
line-length = 160
extend-exclude = [
  "deliverables",
  "generated",
  "release",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B905"]

[tool.ruff.lint.per-file-ignores]
"skill/tests/*.py" = ["E402"]
